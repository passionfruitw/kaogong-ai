[build]
builder = "NIXPACKS"

[deploy]
startCommand = "if [ -d backend ]; then cd backend; fi; python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/api/ai/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
