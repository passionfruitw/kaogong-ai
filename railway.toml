[build]
builder = "NIXPACKS"
buildCommand = "cd 考公AI备考/backend && pip install -r requirements.txt"

[deploy]
startCommand = "cd 考公AI备考/backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PORT = "8000"
