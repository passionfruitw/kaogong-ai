[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "frontend/dist"

[[redirects]]
  from = "/api/*"
  to = "https://kaogong-ai-production.up.railway.app/api/:splat"
  status = 200
  force = true
